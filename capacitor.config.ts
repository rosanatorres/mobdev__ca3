import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'ionic.mobdev_ca3',
  appName: 'mobdev_ca3',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
